% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/merge_key_cols.R
\name{merge_key_cols}
\alias{merge_key_cols}
\title{Merge nested key columns vertically}
\usage{
merge_key_cols(wb, sheetnm, df, cols, start_row)
}
\arguments{
\item{wb}{An openxlsx Workbook object}

\item{sheetnm}{A character string of the sheet name}

\item{df}{A data frame in the same row order it was/will be written to \code{sheetnm}}

\item{cols}{A character vector of column names to merge, outermost first}

\item{start_row}{A number of the sheet row where the first row of \code{df} sits}
}
\value{
Called for its side effect on \code{wb}
}
\description{
Vertically merges each of \code{cols} (in the order given) over the
rows it spans, nested: a later column's run only continues while every
earlier column in \code{cols} is also unchanged. For example, Year only merges
within one Area's block, Station Group only within one (Area, Year).
Plain \code{rle()} on a single column would instead merge across a parent
boundary whenever the value happens to repeat there.
}
\examples{
\dontrun{
merge_key_cols(wb, "Table 1", df, cols = c("Area", "Year"), start_row = 4)
}
}
