% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/record_note_runs.R
\name{record_note_runs}
\alias{record_note_runs}
\title{Record note cells to patch with rich-text runs}
\usage{
record_note_runs(sheet, row, text, regex, style, perl = TRUE)
}
\arguments{
\item{sheet}{A character string of the sheet name}

\item{row}{A number of the row of the note cell (in column A)}

\item{text}{A character string of the full note text}

\item{regex}{A character string of the regular expression matching the
text to style}

\item{style}{A list of formatting arguments passed to \code{openxlsx2::fmt_txt()},
e.g. \code{list(bold = TRUE)} or \code{list(vert_align = "subscript")}}

\item{perl}{A logical of whether \code{regex} is a Perl-compatible regex}
}
\value{
Called for its side effect on \code{note_run_specs}
}
\description{
openxlsx has no rich-text API (\code{createStyle()} styles a whole
cell), and Unicode bold/subscript look-alikes render badly or don't exist
for every letter. Instead, write every note as one plain-text cell as
usual, record here which cells need styled words, and after
\code{saveWorkbook()} patch them into rich-text runs within the same cell with
\code{\link[=apply_note_runs]{apply_note_runs()}}.
}
\details{
Appends to a list named \code{note_run_specs}, which the caller must
create in the global environment first (\code{note_run_specs <- list()}) and
later pass to \code{\link[=apply_note_runs]{apply_note_runs()}}.
}
\examples{
\dontrun{
note_run_specs <- list()
record_note_runs("Table 1", row = 22, text = note[1], regex = "Bold font",
                 style = list(bold = TRUE))
}
}
