% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/richtext_with_highlight.R
\name{richtext_with_highlight}
\alias{richtext_with_highlight}
\title{Build rich text with styled regex matches}
\usage{
richtext_with_highlight(
  text,
  regex,
  style,
  font = "Calibri",
  size = 9,
  perl = TRUE
)
}
\arguments{
\item{text}{A character string of the full cell text}

\item{regex}{A character string of the regular expression matching the
text to style}

\item{style}{A list of formatting arguments passed to \code{openxlsx2::fmt_txt()}
for matched runs}

\item{font}{A character string of the font name for all runs}

\item{size}{A number of the font size for all runs}

\item{perl}{A logical of whether \code{regex} is a Perl-compatible regex}
}
\value{
An openxlsx2 \code{fmt_txt} object
}
\description{
Builds one cell's worth of alternating plain/styled
\code{openxlsx2::fmt_txt()} runs from every match of \code{regex} in \code{text}.
}
\details{
Font and size are set explicitly on every run, not just the styled
ones: rich-text runs don't inherit the cell's style, so a run without them
renders in Excel's default Calibri 11.
}
\examples{
\dontrun{
richtext_with_highlight("Bold font = significant", "Bold font", list(bold = TRUE))
}
}
