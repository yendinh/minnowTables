% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_legend_swatch_shapes.R
\name{add_legend_swatch_shapes}
\alias{add_legend_swatch_shapes}
\title{Draw legend swatch text boxes into a saved workbook}
\usage{
add_legend_swatch_shapes(path, specs, width_in = 0.5, height_in = 0.15)
}
\arguments{
\item{path}{A character string of the saved .xlsx file path}

\item{specs}{A list of specs built by \code{\link[=record_legend_shapes]{record_legend_shapes()}}, usually
\code{legend_shape_specs}}

\item{width_in}{A number of the swatch width in inches}

\item{height_in}{A number of the swatch height in inches}
}
\value{
\code{path}, invisibly
}
\description{
Adds one floating, solid-filled Excel text-box shape per row
recorded by \code{\link[=record_legend_shapes]{record_legend_shapes()}}, centred in column 1 of that row.
openxlsx has no shape API, so this edits the saved .xlsx package directly:
unzip, write a \code{drawingN.xml} part per sheet, wire it up via
\verb{[Content_Types].xml}, the sheet's \verb{_rels} and a \verb{<drawing r:id=".."/>}
tag on the sheet XML, then re-zip.
}
\details{
Run after \code{openxlsx::saveWorkbook()} and before
\code{\link[=repair_workbook_package]{repair_workbook_package()}}, so the new drawing parts are in place when
the repair step's orphan-relationship check runs. Swatch centring assumes
column 1 is 12 characters wide and note rows use the default 15pt height.
}
\examples{
\dontrun{
openxlsx::saveWorkbook(wb, "tables.xlsx", overwrite = TRUE)
add_legend_swatch_shapes("tables.xlsx", legend_shape_specs)
repair_workbook_package("tables.xlsx")
}
}
