% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/merge_identical_rows.R
\name{merge_identical_rows}
\alias{merge_identical_rows}
\title{Merge runs of identical values in one column}
\usage{
merge_identical_rows(wb, sheet, data, col_index, row_start, row_end)
}
\arguments{
\item{wb}{An openxlsx Workbook object}

\item{sheet}{A character string of the sheet name (or its index)}

\item{data}{A data frame whose rows line up with the sheet's rows}

\item{col_index}{A number of the column to merge (in both \code{data} and the sheet)}

\item{row_start}{A number of the first row to consider}

\item{row_end}{A number of the last row to consider}
}
\value{
Called for its side effect on \code{wb}
}
\description{
Vertically merges consecutive cells in a column that hold the
same value. Each column is merged independently; use \code{\link[=merge_key_cols]{merge_key_cols()}}
when a column's runs must not cross a parent column's boundaries.
}
\examples{
\dontrun{
merge_identical_rows(wb, "Table 1", df, col_index = 1, row_start = 4, row_end = 20)
}
}
