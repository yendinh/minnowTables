% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/excel.format.mn.R
\name{excel.format.mn}
\alias{excel.format.mn}
\title{Write and format a table on a new worksheet (Arial)}
\usage{
excel.format.mn(
  wb,
  xx,
  sheetNm,
  header,
  dat.row = 4,
  header.row = c(3, 4),
  merged.cells = c(1, 2),
  thin.lines.cols = 1,
  thin.lines.rows = 3,
  caption,
  col.widths = NULL,
  note = paste0("Note: \\"-\\" = no data."),
  shading.rows = 0
)
}
\arguments{
\item{wb}{An openxlsx Workbook object}

\item{xx}{A data frame of the table body (written without column names)}

\item{sheetNm}{A character string of the new sheet name}

\item{header}{A list of header rows, each a character vector with one entry
per column; element \code{i} is written to row \code{header.row[i]}}

\item{dat.row}{A number of the row the data starts on}

\item{header.row}{A numeric vector of the header rows}

\item{merged.cells}{A numeric vector of columns whose runs of identical values
are merged vertically (\code{NA} to skip merging)}

\item{thin.lines.cols}{A numeric vector of columns that get a thin right border}

\item{thin.lines.rows}{A numeric vector of rows that get a thin bottom border}

\item{caption}{A character string of the table caption, written to row 1}

\item{col.widths}{An optional numeric vector of column widths (same units
passed to \code{openxlsx::setColWidths()}). When given, the caption row is
sized to its real wrapped line count with \code{\link[=estimate_wrapped_lines]{estimate_wrapped_lines()}}
instead of staying clipped to one default-height row}

\item{note}{A character vector of note/footnote lines written below the table}

\item{shading.rows}{A number of the leading \code{note} lines that are
shading-legend lines. Their text is written to column 2 instead of column 1, leaving column 1 clear for a colour swatch drawn there; all other note lines span the full table width}
}
\value{
Called for its side effect on \code{wb}
}
\description{
Adds a worksheet and writes \code{xx} as a formatted table: Arial
font, hairline inner borders, bold header rows, thin separator lines,
vertically merged grouping columns, a medium outer border, a bold wrapped
caption in row 1 and note/footnote rows below the table.
}
\examples{
\dontrun{
wb <- openxlsx::createWorkbook()
excel.format.mn(wb, df, "Table 1",
  header = list(c("Area", "Year", "Mean", "SD")),
  dat.row = 4, header.row = 3, merged.cells = c(1, 2),
  caption = "Table 1. Summary statistics")
}
}
