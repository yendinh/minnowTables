% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/record_legend_shapes.R
\name{record_legend_shapes}
\alias{record_legend_shapes}
\title{Record legend swatch positions for later drawing}
\usage{
record_legend_shapes(sheet, rows, colours)
}
\arguments{
\item{sheet}{A character string of the sheet name}

\item{rows}{A numeric vector of the rows to anchor a swatch on (column 1)}

\item{colours}{A character vector of fill colours, one per row}
}
\value{
Called for its side effect on \code{legend_shape_specs}
}
\description{
openxlsx has no drawing/shape API, so legend swatches can't be
created while building the workbook. Instead each table's swatch
positions/colours are recorded here, and the actual text-box shapes are
drawn into the saved .xlsx afterwards by \code{\link[=add_legend_swatch_shapes]{add_legend_swatch_shapes()}}.
}
\details{
Appends to a list named \code{legend_shape_specs}, which the caller must
create in the global environment first (\code{legend_shape_specs <- list()})
and later pass to \code{\link[=add_legend_swatch_shapes]{add_legend_swatch_shapes()}}.
}
\examples{
\dontrun{
legend_shape_specs <- list()
record_legend_shapes("Table 1", rows = c(22, 23), colours = c("#9BC2E6", "#C4D79B"))
}
}
