% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_wrapped_lines.R
\name{estimate_wrapped_lines}
\alias{estimate_wrapped_lines}
\title{Estimate how many lines text wraps to in an Excel cell}
\usage{
estimate_wrapped_lines(
  texts,
  total_width_px,
  font = "Calibri",
  size = 9,
  margin_px = 10
)
}
\arguments{
\item{texts}{A character vector of texts to measure}

\item{total_width_px}{A number of the available width in pixels, e.g.
\code{sum(excel_width_to_px(col.widths))}}

\item{font}{A character string of the font name}

\item{size}{A number of the font size in points}

\item{margin_px}{A number of pixels subtracted from \code{total_width_px} for
cell padding}
}
\value{
An integer vector of wrapped line counts, one per element of \code{texts}
}
\description{
Measures each text's real wrapped line count using the actual
font/size and merged width in pixels, greedily word-wrapping the same way
Excel does. A fixed "chars per line" ratio either clips a long note or
leaves blank space at the bottom of the row; this avoids both.
}
\details{
Windows only: text widths are measured with
\code{grDevices::windowsFonts()} on an off-screen PNG device.
}
\examples{
\dontrun{
estimate_wrapped_lines("Note: \"-\" = no data.", sum(excel_width_to_px(c(12, 10))))
}
}
