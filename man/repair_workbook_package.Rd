% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/repair_workbook_package.R
\name{repair_workbook_package}
\alias{repair_workbook_package}
\title{Repair a saved workbook so Excel opens it without a repair prompt}
\usage{
repair_workbook_package(path)
}
\arguments{
\item{path}{A character string of the saved .xlsx file path, repaired in place}
}
\value{
\code{path}, invisibly
}
\description{
Cleans invalid parts that openxlsx (4.2.8.1) leaves behind in a
saved .xlsx, each of which triggers Excel's "problem with some content"
repair prompt:
\enumerate{
\item parts with no declared content type (e.g. a stray \verb{[trash]/*.dat} folder)
\item orphaned worksheet relationships pointing at drawing/vmlDrawing files
that were never created
\item duplicate legacy comment VML shapes (duplicate ids, or two shapes
anchored to the same comment cell; the last one is kept)
}
}
\examples{
\dontrun{
openxlsx::saveWorkbook(wb, "tables.xlsx", overwrite = TRUE)
repair_workbook_package("tables.xlsx")
}
}
